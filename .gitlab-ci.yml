image: node:10.13.0

cache:
  paths:
    - node_modules/
    - .yarn

before_script:
  - yarn config set cache-folder .yarn
  - yarn install

tests:
  stage: test
  script:
    - make test

flow:
  stage: test
  script:
    - make less
    - yarn run flow check

lint:
  stage: test
  script:
    - yarn run lint

pages:
  stage: deploy
  script:
    - make gitlab-pages
  artifacts:
    paths:
      - public
  only:
    - master
