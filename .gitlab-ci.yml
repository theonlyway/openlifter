image: node:10.13.0

stages:
  - test

tests:
  stage: test
  cache:
    paths:
      - node_modules/
      - .yarn
  before_script:
    - yarn config set cache-folder .yarn
    - yarn install
  script:
    - make test
